# Handoff 2026-04-17

## Trạng thái hiện tại

- App đã startup thành công với:
  - `provider=groq`
  - `reasoner=analyzer=openai/gpt-oss-20b`
  - `retriever_mode=parent_child_memory`
  - `vector_backend=memory`
- Log đã lưu tại: `logs/app.log` (last update: 2026-04-17 02:05:27).
- Dữ liệu ingest/chunking đã cải thiện:
  - Đã loại bỏ nhiễu `about:blank`.
  - Chunk pháp lý theo cấu trúc Điều/Khoản/Điểm tốt hơn.
  - Parent-child retrieval đã hoạt động ở chế độ memory runtime.

## Kết quả test gần nhất (từ log)

- 14 queries, 13 answers.
- 7 sự kiện `429 Too Many Requests`, có 1 lần fail cứng query.
- Nhiều câu trả lời đúng (Điều 113, Điều 250, nợ lương 2 tháng, trả lương chậm 20 ngày, sa thải tự ý nghỉ).
- Lỗi chất lượng còn tồn tại:
  1. **Over-rejected / từ chối quá an toàn** ở các câu "đơn phương chấm dứt trái luật bồi thường gì?".
  2. Một số câu bị fallback "Không đủ căn cứ..." dù retrieval đã có điều luật liên quan.
  3. Câu "Điều 35 quy định gì?" đôi lúc vẫn hỏi lại không cần thiết.

## Chẩn đoán nguyên nhân chính

- Thiếu bước **disambiguation (ask-back)** trong guard/reasoning:
  - Câu hỏi mơ hồ về chủ thể pháp lý (NLĐ vs NSDLĐ) không được hỏi làm rõ.
  - Hệ thống đang có xu hướng từ chối thay vì trả lời theo 2 nhánh điều kiện + hỏi lại.
- Bị giới hạn TPM ở Groq gây 429 khi chuỗi gọi LLM dày.

## Việc đã làm trong phiên này

- Sửa kết nối Groq để tránh lỗi proxy chặn (`127.0.0.1:9`) trong `llm_factory`.
- Bật offline embedding + memory backend để hệ thống chạy ổn định trong môi trường hiện tại.
- Theo dõi realtime log trong phiên user test và tổng hợp lỗi.

## Kế hoạch ưu tiên cho phiên tiếp theo

### P0

1. Nâng guard từ 2 trạng thái thành 3 trạng thái:
   - `answer`
   - `ask_back`
   - `insufficient_context`
2. Triển khai ambiguity detector cho nhóm câu:
   - "đơn phương chấm dứt trái luật bồi thường gì"
   - ưu tiên map Điều 40/41.
3. Nếu mơ hồ chủ thể:
   - Trả lời ngắn theo 2 nhánh (NLĐ / NSDLĐ),
   - Kèm câu hỏi làm rõ để chốt nhánh.
   - Không fallback từ chối ngay.

### P1

1. Lưu "role/chủ thể pháp lý" vào conversation memory để giảm hỏi lặp.
2. Thêm retry/backoff + degrade context cho 429 để tránh fail cứng.

### P2

1. Bổ sung evaluation rubric:
   - `over_reject_rate`
   - `ask_back_rate`
   - `conditional_answer_success`
2. Tạo bộ test riêng cho ambiguity/legal-role disambiguation.

## Ghi chú vận hành

- Chạy app:
  - `python -m streamlit run app.py --server.port 8511`
- Nếu gặp lỗi mạng bất thường với Groq, kiểm tra env proxy trong terminal hiện tại.

