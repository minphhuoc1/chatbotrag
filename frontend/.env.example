# Default: mock mode for UI preview without starting Python backend.
USE_MOCK=true

# Set USE_MOCK=false and start the Python API server to use the real RAG engine.
BACKEND_URL=http://127.0.0.1:8007

# Optional: increase when the LLM provider is slow or rate-limited.
BACKEND_TIMEOUT_MS=60000
